name: cd

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23.0"

      - name: Build app
        run: ./scripts/buildprod.sh
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Use gcloud CLI
        run: gcloud info

      - name: Build and push Docker Image
        run: gcloud builds submit --tag us-central1-docker.pkg.dev/encoded-adviser-452421-d1/notely-ar-repo/my-image:latest .
      - name: Deploy to Cloud Run
<<<<<<< HEAD
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/encoded-adviser-452421-d1/notely-ar-repo/my-image:latest --region us-central1 --allow-unauthenticated --project encoded-adviser-452421-d1 --max-instances=4
=======
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/encoded-adviser-452421-d1/notely-ar-repo/my-image:latest --region REGION --allow-unauthenticated --project encoded-adviser-452421-d1 --max-instances=4
>>>>>>> ec93bd2cd5f1aa01c8e3c471f571fff2768dd588
